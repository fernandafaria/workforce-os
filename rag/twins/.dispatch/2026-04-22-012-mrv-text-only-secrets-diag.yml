# Full-mode TEXT-ONLY v4 — Twin #1 (Rubens Menin / MRV).
#
# Dispatch 011 revealed: Verify specs now runs (verify_specs_exit=0,
# 21 sources parsed) but `Run twins pipeline` step never executes, and
# its /tmp log is never created. The step between them is
# `Verify required secrets (full mode only)` — which was silent on
# success AND failure.
#
# This dispatch adds logging + proper secret requirements for text-only
# mode (FIRECRAWL replaces OPENAI when skip_transcribe=true) so the
# next PR comment reveals which secret is missing (if any).
#
# Also cleans /tmp logs at job start — self-hosted runner was
# accumulating cross-run log state (Resolve log showed dispatches 010
# AND 011 on the dispatch-011 run).

slug: mrv-rubens-menin
mode: full
confirm_full_mode: true
skip_transcribe: true
