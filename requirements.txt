# Workforce OS — Python dependencies
# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.0
pydantic-settings>=2.0

# Supabase (used directly by AgentCatalog, MemoryPipeline, auth)
supabase>=2.0

# LangGraph (orchestration)
langgraph>=0.2.0
langgraph-checkpoint>=2.0

# FastMCP (MCP server)
fastmcp>=2.0

# LangChain (RAG)
langchain>=0.3.0
langchain-core>=0.3.0

# Templates
jinja2>=3.1

# HTTP client (async LLM + Voyage calls)
httpx>=0.28.0

# YAML (rituals)
pyyaml>=6.0

# Rate limiting (Upstash Redis-compatible)
slowapi>=0.1.9

# Twins pipeline (ingest, transcribe, embed)
firecrawl-py>=1.5.0,<2.0.0  # URL scraping para ingest_person
openai-whisper>=20240930    # Transcrição de áudio (modelo turbo)
numpy>=1.24.0               # dependência do whisper
